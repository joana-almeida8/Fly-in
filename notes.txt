pydantic_errors = [
    {'key': [error1, error2, error3]}
    {'key': [error1, error2, error3]}
]

pydatic (mode=before) --> recebe a linha e separa:
 - key
 - name
 - x (optional)
 - y (optional)
 - metadata (optional)

 pydantic class --> verifica o tipo de variavel

 pydantic (mode=after) --> verificacoes posteriores consoante a key

 o parser verifica linha a linha e adiciona a linha validada ao parsed_data dict

 parsed_data = {
    'start': [
        {
            'key': start_hub
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        }
    ],
    'end': [
        {
            'key': end_hub
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        }
    ],
    'hubs': [
        {
            'key': hub
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        },
        {
            'key': hub
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        },
        {
            'key': hub
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        }
    ],
    'connection': [
        {
            'key': connect
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        },
        {
            'key': connect
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        },
        {
            'key': connect
            'name' = name,
            'coordenades' = (x, y)
            'metadata' = metadata
        }
    ]
}



Questions:
- Can BaseModel classes raise ValueError instead of ValidationError?
- 