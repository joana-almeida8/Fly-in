from ..map.network import Network, Hub
from .simulation import Drone

def scheduler(network: Network, drone: Drone) -> list[list[str]]:
    '''Schedule drone moves considering metadata restrictions'''
    ...
